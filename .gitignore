.DS_Store
**/.DS_Store
installed_plugins.json
installed_plugins_v2.json
marketplaces/

# Python cache
__pycache__/
*.pyc

# Project memory (each project has its own)
.claude/memory/

# Session state (runtime, not project data)
.claude/sessions/

# Auto-generated context files (runtime, not project data)
.claude/rules/session-narrative.md

# Local settings (user-specific overrides)
*.local.json

# Flow state (never commit)
dev/
